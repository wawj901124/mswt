75.排序
    从小到大排序：sorted(list)
    从大到小排序：sorted(list,reverse=True)

76.冒泡
    1.比较相邻的元素，如果第一个比第二个大，就交换
    2.一轮遍历，每相邻元素，重复1，最大放队尾
    3.不包括已经排队尾的，重复2

77.快排
    1.从列表中挑出一个元素，作为基准值key
    2.所有小于key的元素放左边，所有大于key的元素放右边
    3.分别递归左侧列表，右侧列表

78.堆排序
    堆排序指利用堆的数据结构设计的一种排序算法
    堆近似一个完全二叉树结构
    子结点的键值小于（或者大于）它的父节点

79.二分查找
     二分查找又称折半查找
     必须采用顺序存储结构
     必须按关键字大小有序排列

80.素数
    素数又称质数
    0，1不是素数
    除了1和它自身外，不能被其他自然数整除的数