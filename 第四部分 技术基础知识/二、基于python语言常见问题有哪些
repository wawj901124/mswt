60.说一下面向对象的概念？
    面向对象编程，简称OOP，是一种程序设计思想
    主要包括：类、继承、多态、实例、属性、方法

61.什么是进程、线程、协程？
    进程：独立数据空间，进程间不共享数据，系统调度。
    线程：执行程序的最小单元，进行内线程间共享资源，系统调度。
    协程：程序员调度，分解一个线程成为多个“微线程”。

62.如何使用python实现socket编程？
    Socket又称套接字，应用程序通过套接字向网络发出请求
    应用程序通过套接字应答网络请求
    使主机间或者一台计算机上的进程间可以通讯
    
63.什么是lambda函数？
    匿名函数，即没有函数名的函数

64.tuple和list什么区别？
    tuple为元组，list为列表
    tuple的元素不可该
    list的元素可修改

65.range函数的用法？
    返回一系列连续增加的整数
    工作方式类似于分片
    可以生成一个列表对象

66.字符串的拆分方法有哪些？
    string对象的split方法，不允许有多个分隔符
    函数re.split(),允许为分割符指定多个正则模式

67.单引号、双引号、三引号的区别？
    单引号，双引号没有区别，都可以用来表示字符串
    三引号，字符串内可以直接换行

68.*args,**kwargs什么作用？
    *args,可变的参数列表
    **kwargs,键值对参数列表

69.python中pass语句的作用？
    占位符，实现一个空函数

70.re模块中match和search方法的不同？
    search,扫描整个字符串查找匹配
    match，只在字符串的开始位置匹配

71.解释一下WSGI和FastCGI的关系？
    CGI:公共网关接口（CommonGateway Interface）
    HTTP服务器与机器上的程序进行交互的一种工具
    程序须运行在网络服务器上，和语言无关

    WSGI：Python Web Server Gateway Interface
    Python 应用程序和WEB服务器中间的一种接口
    FastCGI像是一个常驻（long-live）型的CGI，它可以一直执行着

72.python是如何操作文件的？
    打开文件
    写入或读取数据
    关闭文件

73.python的内存管理机制？
    引用计数
    垃圾回收
    内存池

74.dict的items与iteritems区别？
    items方法以列表方式返回，返回时没有特殊顺序
    iteritems方法类似，但返回的是一个迭代器对象